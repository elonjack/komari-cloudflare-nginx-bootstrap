# 更新日志

本文件记录项目的重要变更。

## 1.3.1 — 2026-08-13

- 菜单前显示当前脚本版本号，便于确认本机是否已更新。
- 操作菜单、确认、域名输入等需要用户注意的交互提示统一使用黄色；信息、成功、警告和错误仍保留各自语义颜色。

## 1.3.0 — 2026-08-13

- 脚本启动后新增“安装或重新加固 / 安全更新已有面板”菜单，也可用 `--install` 或 `--update` 直接进入对应流程。
- 新增受控 Komari 面板更新：只拉取官方 `latest` 镜像，始终保持 `127.0.0.1:25774:25774` 绑定，不会重新暴露面板端口。
- 更新前自动创建仅 root 可读的数据归档；新版容器未通过健康检查时自动恢复数据和旧容器。
- README 补充菜单式安装、面板更新、回滚边界与 Agent/面板更新的区别。

## 1.2.0 — 2026-08-10

- 交互式终端中使用一致的颜色语义：信息为青色、成功为绿色、警告为黄色、错误为红色；日志和 CI 环境仍保持纯文本。
- 源证书与私钥使用文档推荐路径时可自动识别；新增短命令与 `--enable-aop`，同时保留完整、可审计的部署写法。

## 1.1.2 — 2026-08-10

- Komari 启动后最多等待约 60 秒再判定健康检查失败，避免初始化期间连接重置导致误报。
- 已存在旧容器时，新的健康检查失败会自动恢复旧容器。

## 1.1.1 — 2026-08-10

- 将 README、安全策略、更新日志和脚本交互提示完整中文化。
- 统一运行时的【信息 / 警告 / 错误】提示，并在确认前展示操作范围。

## 1.1.0 — 2026-08-10

- 默认使用仅 HTTPS 的 Nginx 反向代理；Komari 不再有 HTTP 监听入口。
- 增加保守的响应安全头、可选 Cloudflare AOP 配置和安全加固清单。
- 增加可直接复制的小白操作说明，涵盖 Cloudflare 证书安全保存和交互式启动。
- 将 README 与脚本的交互提示统一为中文。

## 1.0.0 — 2026-08-10

- 首次发布 Debian 13 环境下的 Komari、Nginx 与 Cloudflare 源证书部署脚本。
